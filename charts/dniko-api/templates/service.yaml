apiVersion: v1
kind: Service
metadata:
  name: {{ include "dNiko-core-api.fullname" . }}
  labels:
    {{- include "dNiko-core-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dNiko-core-api.selectorLabels" . | nindent 4 }}
