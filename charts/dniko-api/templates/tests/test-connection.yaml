apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dNiko-core-api.fullname" . }}-test-connection"
  labels:
    {{- include "dNiko-core-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dNiko-core-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
