apiVersion: v1
kind: Service
metadata:
  name: {{ include "ShortLinks.fullname" . }}
  namespace: {{ include "ShortLinks.namespace" . }}
  labels:
    {{- include "ShortLinks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ShortLinks.selectorLabels" . | nindent 4 }}
