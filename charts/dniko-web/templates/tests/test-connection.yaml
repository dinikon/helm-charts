apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ShortLinks.fullname" . }}-test-connection"
  labels:
    {{- include "ShortLinks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ShortLinks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
