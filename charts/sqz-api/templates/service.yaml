apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqz-api.fullname" . }}
  labels:
    {{- include "sqz-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqz-api.selectorLabels" . | nindent 4 }}
