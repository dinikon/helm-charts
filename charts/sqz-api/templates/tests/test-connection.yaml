apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sqz-api.fullname" . }}-test-connection"
  labels:
    {{- include "sqz-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sqz-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
