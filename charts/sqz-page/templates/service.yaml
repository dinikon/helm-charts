apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqz-page.fullname" . }}
  labels:
    {{- include "sqz-page.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqz-page.selectorLabels" . | nindent 4 }}
