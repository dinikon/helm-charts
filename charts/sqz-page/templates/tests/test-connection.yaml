apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sqz-page.fullname" . }}-test-connection"
  labels:
    {{- include "sqz-page.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sqz-page.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
